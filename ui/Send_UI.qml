import QtQuick 2.15
import QtQuick.Controls.Basic

Item {
    width: parent.width
    height: parent.height

    Rectangle {
        anchors.fill: parent
        anchors.centerIn: parent
        color: "#141414"

        ListView {
            //model:
        }

        }
    }
