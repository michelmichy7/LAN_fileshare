import QtQuick 2.15
import QtQuick.Controls.Basic

Rectangle {
    anchors.centerIn: parent
    color: "#414141"

    Text {
        anchors.centerIn: parent
        text: "Waiting for response from another computer..."
    }
}
